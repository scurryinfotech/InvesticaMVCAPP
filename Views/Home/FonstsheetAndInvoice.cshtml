<div class="page active" data-page="3">
    <div class="card fs-card">

        <h1 class="fs-title">Frontsheet &amp; Invoice Details</h1>

        <p class="fs-subtitle">
            This step displays the data that will be used to populate the Frontsheet and Invoice pages.
            All editable inputs were removed — values are fetched from Company Info (previous step) and left-panel filters / master-data.
        </p>

        <!-- GRID -->
        <div class="fs-grid">

            <div class="fs-item">
                <label class="fs-label">Company Name</label>
                <div id="fs_company" class="fs-box">—</div>
            </div>

            <div class="fs-item">
                <label class="fs-label">Company Address</label>
                <div id="fs_address" class="fs-box">—</div>
            </div>

            <div class="fs-item">
                <label class="fs-label">Contact / Phone</label>
                <div id="fs_phone" class="fs-box">—</div>
            </div>

            <div class="fs-item">
                <label class="fs-label">Email</label>
                <div id="fs_email" class="fs-box">—</div>
            </div>

            <div class="fs-item">
                <label class="fs-label">GSTIN</label>
                <div id="fs_gstin" class="fs-box">—</div>
            </div>

            <div class="fs-item">
                <label class="fs-label">PAN</label>
                <div id="fs_pan" class="fs-box">—</div>
            </div>

            <div class="fs-item">
                <label class="fs-label">Entity Type</label>
                <div id="fs_entity" class="fs-box">—</div>
            </div>

            <div class="fs-item">
                <label class="fs-label">Product / Service</label>
                <div id="fs_product" class="fs-box">—</div>
            </div>

            <div class="fs-item fs-wide">
                <label class="fs-label">Location (Area / Ward / Zone)</label>
                <div id="fs_location" class="fs-box">—</div>
            </div>

            <div class="fs-item">
                <label class="fs-label">License Type (from Company)</label>
                <div id="fs_license" class="fs-box">—</div>
            </div>

            <div class="fs-item">
                <label class="fs-label">Status (from Company)</label>
                <div id="fs_status" class="fs-box">Active</div>
            </div>

        </div>

        <!-- BUTTONS -->
        <div class="fs-actions">
            <button class="btn btn-secondary px-4 fw-bold" type="button" onclick="populateFrontsheetInvoiceFields()">
                Fetch Data
            </button>

            <button class="btn btn-warning px-4 fw-bold" type="button" onclick="fillSummary(); showPage(4)">
                Next
            </button>

            <button class="btn btn-outline-dark px-4 fw-bold" type="button" onclick="showPage(2)">
                Back
            </button>
        </div>

    </div>
</div>
